const endpoints = {
  AUTH: "/auth",
  SERVICES: "/services",
  SCHEDULES: "/schedules",
  ENROLLMENTS: "/enrollments",
  VERIFICATIONS: "/verifications",
  USERS: "/users",
};

export default endpoints;
